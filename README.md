# 植保飞行资料

这里集中维护田块边界、敏感区域、药剂标签和离线航迹示例。坐标统一使用 WGS84，经纬度数组首尾闭合；剂量使用毫升每亩，距离使用米。

`reference/domain.json` 中每个航迹点同时包含设备时间和接收时间。药箱编号代表一次装载事实，换箱后即使药剂相同也使用新的编号。敏感区域的缓冲距离来自其自身类别。

资料自检命令如下：

```bash
python -m unittest discover -s tests
```
